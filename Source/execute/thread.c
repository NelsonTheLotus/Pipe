#include "thread.h"
// Thread is the sequencial execution unit.
// It has it's own dedicated sequencial shell
// and it manages it's  command sequence, feeding the shell and 
// linking IO pairs through the shell's IO capture.